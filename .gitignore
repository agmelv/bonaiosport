node_modules/
.env
*.log
.DS_Store
scratch/

# The data directory of a run from source: profiles, and now the saved
# catalog, cards and logos -- hundreds of megabytes of them.
data/

# Build output. Was committed once and went stale, shipping a bundle that
# predated the whole login system to anyone running it without Docker.
dist/

# Build output from the wasm step, committed once by accident.
dist_wasm/

# No binaries in the repo. A 53 MB cloudflared.exe lived here, which is both
# dead weight and something nobody cloning this should be asked to trust.
*.exe
